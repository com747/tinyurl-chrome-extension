{
    "version": "1.0",
"manifest_version": 2,
 
    "name": "TinyURL", 
    "description": "Make TinyURLs without visiting the TinyURL website and all the other extensions don't allow a custom alias but this one does!",
    "browser_action": {
        "default_icon": "icon.png",
        "default_popup": "popup.html"
    },
    "permissions": [
        "activeTab"
     ]
}